import * as myModule from "./module.mjs"

const log = console.log;

log(myModule.next());
log(myModule.next());
log(myModule.next());
log(myModule.next());
log(myModule.prve());
log(myModule.square());
log(myModule.prve());
